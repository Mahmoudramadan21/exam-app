import { createImmutableVariants } from "@/shared/lib/utils/create-immutable-variants";

// ===== Immutable variants for each entity =====
export const IMMUTABLE_DIPLOMA_VARIANTS = createImmutableVariants("diploma");

export const IMMUTABLE_EXAM_VARIANTS = createImmutableVariants("exam");

export const IMMUTABLE_QUESTION_VARIANTS = createImmutableVariants("question");
